{% extends 'base.html.twig' %}
{% block title %} {% endblock %}
{% block body %}

    <div class="row">
        <div class="col-md-6">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        <span class="glyphicon glyphicon-bookmark"></span> Quick Shortcuts</h3>
                </div>
                <div class="panel-body">
                    <div class="row">
                        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

                        <canvas id="monthlySalesChart" width="600px" height="500px"></canvas>
                        <script>
                            document.addEventListener('DOMContentLoaded', function () {
                                var monthlySalesData = {{ sales|json_encode|raw }};
                                var ctx = document.getElementById('monthlySalesChart').getContext('2d');

                                var datasets = {};
                                monthlySalesData.forEach(function (entry) {
                                    if (!datasets[entry.productName]) {
                                        datasets[entry.productName] = {
                                            label: entry.productName,
                                            data: [],
                                            backgroundColor: 'rgba(' + Math.floor(Math.random() * 256) + ',' + Math.floor(Math.random() * 256) + ',' + Math.floor(Math.random() * 256) + ',0.5)',
                                        };
                                    }

                                    datasets[entry.productName].data.push(entry.totalSales);
                                });

                                var chartData = {
                                    labels: monthlySalesData.map(function (entry) {
                                        return  entry.month;
                                    }),
                                    datasets: Object.values(datasets),
                                };
                                var chartConfig = {
                                    type: 'bar',
                                    data: chartData,
                                    options: {
                                        scales: {
                                            y: {
                                                beginAtZero: true,
                                            },
                                        },
                                    },
                                };

                                var myChart = new Chart(ctx, chartConfig);
                            });
                        </script>
                        {#                        <div class="col-xs-6 col-md-6">#}
{#                            <a href="#" class="btn btn-success btn-lg" role="button"><span class="glyphicon glyphicon-user"></span> <br/>Users</a>#}
{#                            <a href="#" class="btn btn-info btn-lg" role="button"><span class="glyphicon glyphicon-file"></span> <br/>Notes</a>#}
{#                            <a href="#" class="btn btn-primary btn-lg" role="button"><span class="glyphicon glyphicon-picture"></span> <br/>Photos</a>#}
{#                            <a href="#" class="btn btn-primary btn-lg" role="button"><span class="glyphicon glyphicon-tag"></span> <br/>Tags</a>#}
{#                        </div>#}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}