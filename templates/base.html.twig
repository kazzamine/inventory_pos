<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>  {% block title %}{% endblock %}</title>
        <link rel="icon" type="image/png" href="{{ asset('build/images/icon.13da4f47.png') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('appstyle') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
            {{ encore_entry_script_tags('adminEventListner') }}
        {% endblock %}
    </head>
    <body>
    <div class="container-fluid">
        <div class="row flex-nowrap">
        {#including navbar #}
    {% include 'attachements/navbar.html.twig' %}
                <div class="col py-3" id="content">
                    {% block body %}

                    {% endblock %}
                </div>
            </div>
        </div>
{#    {% include 'attachements/footer.html.twig' %}#}
    </body>
</html>
